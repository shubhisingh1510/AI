# Raw dataset images are third-party medical data. Currently: "Lower Limb and Feet Wound
# Image Dataset for Medical Analysis" (Mendeley Data, DOI 10.17632/hsj38fwnvr, CC BY 4.0).
# Not committed simply to keep the repo small/fast to clone -- run scripts/fetch_dataset.py
# to repopulate data/raw/ (normal/, wound/) from the public, checksum-verified source.
data/raw/
data/_download/
data/raw_dfu_archive/

# Local virtualenv
.venv/

# Trained model weights / large artifacts (regenerable by running the pipeline)
results/classical_backbone_state.pt
results/classical_features.npy

__pycache__/
*.pyc

# Raw stdout/stderr logs from manual training runs
*.log
